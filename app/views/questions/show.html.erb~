<div class="container-fluid">
	<div class="row-fluid">
		<div class="span2></div>
		<div class="well span8">
			<h1>Question: <small><%= @question.title %></small></h1>

			<h4>Options</h4>

			<table class="table table-hover">
  			<thead>
					<th>Option</th>
					<th>Correct option?</th>
					<th></th>
				</thead>
				<tbody>
			  <% @question.choices.each do |choice| %>
				 <tr>
					<td><%= choice.text %></td>
					<td class="add-on"><% if choice.correct %><% end %></td>
					<td><%= link_to 'Delete', [@question, choice], :method => :delete, :confirm => "Are you sure?", :class => 'btn btn-danger' %></td>
				 </tr>
			  <% end %>
				</tbody>
			</table>

			<h5>New Choice</h5>
			<%= form_tag({:controller => "choices", :action => "create"}) do %>
 				<%= hidden_field_tag :question, @question.id %>
			  <%= label_tag :text, "Text" %><br />
			  <%= text_area_tag :text %>
			  <br />
			  <%= check_box_tag :correct %>
			  <%= label_tag :correct, "Correct" %>
			  <br />
			  <%= submit_tag "Create" %>
			<% end %>
		</div>
		<div class="span2></div>
	</div>
</div>
<div class="form-actions">
<%= link_to 'Edit', edit_question_path(@question) %>
<%= link_to 'Back', questions_path %>
</div>
<% content_for(:title) do %>
  Question Details
<% end %>
