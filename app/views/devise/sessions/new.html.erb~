<div class="container-fluid">
  <div class="row-fluid">
    <div class="span2"></div>
    <%= form_for(resource, :as => resource_name, :url => session_path(resource_name), :html => { class: "form-horizontal" }) do |f| %>
      <div class="well span8">
        <div id="legend">
          <legend class="">Sign in</legend>
        </div>

        <div class="control-group">
          <%= f.label :email, class: "control-label" %>
          <div class="controls">
            <%= f.email_field :email, class: "input-fit" %>
            <p class="help-inline">We need to know what your identity is...</p>
          </div>
        </div>

        <div class="control-group">
          <%= f.label :password, class: "control-label" %>
          <div class="controls">
            <%= f.password_field :password, class: "input-fit" %>
            <p class="help-inline">We need to ensure you are the correct person!</p>
          </div>
        </div>

        <% if devise_mapping.rememberable? %>
          <div class="control-group">          
            <div class="controls">
              <div class="checkbox">
                <%= f.check_box :remember_me %> Remember me
              </div>
            </div>
          </div>
        <% end %>

        <div class="form-actions">
          <%= f.submit "Sign in", class: "btn btn-primary" %>
          <br />
          <%= render "devise/shared/links" %>
        </div>
      </div>
    <% end %>
    <div class="span2"></div>
  </div>
</div>
<div class="form-actions">
  <div class="span4"></div>
  
  <div class="span4"></div>
</div>
